name: Run

on: 
  push: 
    branches: [master]
  pull_request: 
    branches: [master]

jobs:
  builds:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - run: ./build.sh
